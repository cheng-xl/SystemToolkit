<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SystemToolkit">

        <!-- 主仪表盘 -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 微信通知提醒 -->
        <activity
            android:name=".feature.wechat.WeChatNotifierActivity"
            android:exported="false"
            android:label="微信通知提醒"
            android:parentActivityName=".MainActivity" />

        <!-- 系统更新屏蔽 -->
        <activity
            android:name=".feature.updateblocker.UpdateBlockerActivity"
            android:exported="false"
            android:label="系统更新屏蔽"
            android:parentActivityName=".MainActivity" />

        <!-- 通知监听服务 -->
        <service
            android:name=".feature.wechat.WeChatNotificationService"
            android:exported="true"
            android:foregroundServiceType="dataSync"
            android:description="@string/service_description"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

    </application>

</manifest>
